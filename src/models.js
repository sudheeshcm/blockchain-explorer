import accounts from '@Scenes/Accounts/model';
import notification from '@Components/Notification/model';
import dialog from '@Components/Dialog/model';

export default {
  accounts,
  dialog,
  notification,
};
